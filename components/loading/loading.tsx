
export default function Loading() {
    return (
        <section className="w-full min-h-screen flex flex-col items-center px-4 md:px-20 py-30">
            <h2 className="text-[clamp(2.5rem,6vw,4rem)] leading-tight font-bold mainFont my-auto">Loading...</h2>
        </section>
    )
}